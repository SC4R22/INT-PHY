// This file is intentionally empty — JsonLd is now inlined in app/layout.tsx
// as a plain server-rendered script to avoid Webpack chunk boundary issues
// with 'use client' components inside the root layout.
export {}
