'use client'

// This file is kept for reference but is no longer used.
// Layout is now handled by ServerLayout in components/server-layout.tsx
export function ConditionalLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
